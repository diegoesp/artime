Hi <%= @manager_user.first_name %>,

This is a notification from artimeapp.com

<%= @adder_user.name %> has added the task "<%= @project_task.task.name %>" to the project "<%= @project_task.project.name %>". 

From now on, all users assigned to this project will be able to input to this task and you won't receive any additional alerts.

See ya!